{{#if post.author}}
  {{assign 'author' post.author}}
{{else if page.author}}
  {{assign 'author' page.author}}
{{else}}
  {{assign 'author' site.author}}
{{/if}}
{{#if author}}<span class="byline author vcard">Posted by <span class="fn">{{ author }}</span></span>{{/if}}
